---
title: Downloading the ISO
description: Where to get Larch and how to verify it
---

<Callout type="warn">
  Outline only. Larch doesn't have a public release process yet; ISOs are currently built locally. Fill this in once builds are published somewhere (GitHub Releases is the likely candidate, given the repo already lives at `github.com/larch-os/larch`).
</Callout>

## What this page needs once releases exist

- Where to download the ISO (release page link).
- How to verify it (checksum, and whether builds get GPG-signed, since `mkarchiso` supports it and the project currently doesn't use it).
- Minimum specs to run the live session (RAM, GPU requirements, especially since niri needs a real GPU render node, not just a display).
- How to write the ISO to a USB drive (`dd`, Ventoy, whatever the project wants to recommend).
