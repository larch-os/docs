---
title: Networking
description: NetworkManager, and a VM quirk worth knowing about
---

Larch uses NetworkManager, not `systemd-networkd`/`iwd`. This isn't the default for a minimal Arch setup, it's a deliberate choice because noctalia's network panel needs NetworkManager specifically to show its Wi-Fi/Ethernet toggle.
